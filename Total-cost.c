#include <stdio.h>

int main(){
    //Total cost Calculation
    int unit_price = 1500;
    int quantity = 2000;
    int total_cost = unit_price * quantity;


    printf("the total cost of items = %d\n", total_cost);

return 0;
}